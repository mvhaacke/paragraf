# Lessons

_Updated after corrections. Reviewed at session start._

## Stripe API version
Always check the installed stripe package for the current apiVersion type —
it changes between major versions. Use the version the types expect, not a
hardcoded one from docs/memory.